package room

import (
	"checkers-backend/game"
	"checkers-backend/player"
)

// handleCapturePiece made by Player `p` against `opponent`. Returns TRUE if all is OK. Else returns FALSE.
func handleCapturePiece(basePayload *game.BasePayload, gameMap map[int32]*game.Piece, p *player.Player, opponent *player.Player) bool {

	return true
}
